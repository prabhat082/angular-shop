import { CatogeryTitlePipe } from './catogery-title.pipe';

describe('CatogeryTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CatogeryTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
